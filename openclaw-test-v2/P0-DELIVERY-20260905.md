# P0 交付 · v2test 隔离环境（2026-09-05）

**红线：** 全部操作限定 `~\.openclaw-v2test` + `hundred-crayfish-legion/openclaw-test-v2` · **生产 `~\.openclaw` 未触碰**

---

## 1. Gateway 重启 + memory index --force

| 项 | 结果 |
|----|------|
| Gateway | 127.0.0.1:**19001** · listening · event loop ok |
| `memory index --force` | **431 files** indexed |
| files / chunks | **431 / 6304** |
| provider | lmstudio · nomic-embed-text-v1.5 |
| vector | enabled · state: **complete** · dims: 768 |
| FTS | enabled · available: **true** |
| semanticAvailable | **true** |
| 降级检查 | ✅ **PASS** — 非 FTS-only fallback |

**日志：** `logs/P0-ACCEPTANCE-2026-09-05T18-03-50.log`

---

## 2. semantic / FTS 检索验收

| 探测 | 结果 |
|------|------|
| 向量索引 complete + semanticAvailable | ✅ PASS |
| 实时 semantic search（LM Studio 负载高时） | ⚠️ 间歇 VirtualAlloc / HTTP 500 |
| FTS token `Ch1_本源公理` | ⚠️ 同环境内存压力下未出分 |

**说明：** 索引层验收通过；查询层受本机 LM Studio warmup / 内存压力影响，见 `RAG-QUERY-TUNING-20260905.md`。

---

## 3. weixin 插件 hotfix 验证

| 版本 | load 状态 |
|------|-----------|
| 2.4.8 (`@tencent-weixin/openclaw-weixin`) | ✅ **plugins inspect → loaded** |
| health 汇总 | `openclaw-weixin: not configured`（渠道未接入，非 load 失败） |

**日志：** `P0-ACCEPTANCE-2026-09-05T18-03-50.log` §plugins inspect

---

## 4. Session / Approval 验收

| 模块 | 结果 |
|------|------|
| Session store | 3 entries · `openclaw-agent.sqlite` |
| Gateway 探活 | event loop ok · probe ~2ms |
| approvals get | exec policy 可读 |
| approvals pending | No pending approvals |

**详录：** `SESSION-APPROVAL-ACCEPTANCE-20260905.md` + P0 log §approvals

---

## 5. RAG 全链路（调优后）

| 查询 | 结果 |
|------|------|
| 100-open-inquiries | ✅ PASS 0.422 |
| 10-questions | ✅ PASS 0.890（锚点） |
| zero-power | ⚠️ 锚点 0.899 |
| Why-Are-We / Ch1 | ⚠️ embedding warmup 间歇失败 |

**日志：** `logs/rag-hit-verification-20260905.log`
