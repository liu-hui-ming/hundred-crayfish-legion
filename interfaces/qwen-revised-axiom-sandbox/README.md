本目录内容仅为辅助接口，若与 /core-axioms/ 核心公理卷宗产生任何冲突，一律以核心公理卷宗为准。

# qwen-revised-axiom-sandbox

定位：碳硅道统合规 0→1 算力实证载体（通义千问审计重构版 Axiom 观测沙盒）。
层级隔离：本目录严格只读引用 /core-axioms/，无写入权限。
导出约束：报告导出内置关键词熔断，物理阻断跨层推导。
冲突优先级：T-02 > Y-04 > P_s。发生 LogicalExclusion 或 PriorityInversion 时，接口文件失效并触发三级熔断。
观测边界：工具层恪守观测边界，不越界干预核心公理。
CI 门控：`ci_check/` 纳入路径保护；迭代合入主分支前须通过全量审计用例。
