# Axium 升维 · 前置依赖清单（铺垫用）

本文件为 **P2 弹性** 可选项：不阻塞 P1 与主仓发版。用于在正式升维/集成前，把依赖一次扫全；条目按你们实际 Axium 线修订即可。

**简单对齐（本阶段已落笔，剩余为运行面 / 会签）：**

| # | 类别 | 检查项 | 状态（✓/空白/备注） |
|---|------|--------|---------------------|
| 1 | 仓与事实源 | 与 `liu-hui-ming/hundred-crayfish-legion` 或 Axium 侧主仓库 **tag/commit** 已对齐、SSOT 无分叉说明 | ✓ **HCL** 对外 SSOT：`https://github.com/liu-hui-ming/hundred-crayfish-legion`；Axium 侧 commit 由联调会签后补一行。 |
| 2 | 身份与秘钥 | PAT / 工作负载身份 / 网关**可用且最小权限**；**禁止** `/path` 类假占位路径（含 `HERMES_HOME` 等可写真路径已验收） | 待运行面：`PAT`/网关由操作者配置；本仓**不**存密钥。路径类以**可写真目录**为准。 |
| 3 | 网络与拓扑 | `OPENCLAW_URL`（及同类联调面）与**实际运行**同一拓扑、TLS/证书/防火墙**已开白** | 待验收：脚本 `scripts/run-genesis-n-rounds.sh` 在设置 `OPENCLAW_URL` 时对目标**探活**；未设则仅探 HCL。 |
| 4 | 数据与归档 | 归档/日志根路径可写、**磁盘/配额**满足最小一轮升维 + 回滚 | 待运维：填实际 `HERMES_HOME`/盘符。 |
| 5 | 人协与门控 | 与 Axium/同盟相关 **owner/评审** 在窗口内可用；**发布窗口** 已登记 | 待行政/项目：填姓名/日期。 |
| 6 | 观测与回退 | 关键指标/报警能覆盖本次变更；**回退命令/镜像 tag** 已记一条 | 待运维：填监控入口与回退 tag。 |
| 7 | 合规与对外 | 若对外的发帖/Issue/标签（如 `[P1-Roadmap]` / `[Manifesto]`）有联动，**链接已备案** | ✓ **草稿**：`docs/issue-exports/`、`docs/github-issue-*`；正式发布后在此格**粘贴 Issue URL**。 |

**说明**

- 升维的**技术细节**以 Axium/同盟双方工程文档为准；上表是 **checklist 骨架**。
- 本清单**不要求**在本仓写业务代码，完成度以「**是否填完并评审过**」为准。
